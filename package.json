{
  "name": "tienda360-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "pnpm -r --parallel dev",
    "dev:turbo": "turbo run dev --ui=stream",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "packageManager": "pnpm@11.22.0",
  "devDependencies": {
    "turbo": "^2.4.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
